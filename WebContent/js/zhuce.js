 window.onload = function(c){
	 if(c==1)
       alert("此账号已注册，请直接登录")
       if(c==0)
    	   alert("注册成功，请登录")
    	 if(c==-1)
    		 alert("未出现异常，但是添加用户失败")
    		 if(c==-1)
    			 alert("添加出现异常")
    }